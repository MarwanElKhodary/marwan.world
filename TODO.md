# TODOs

- Fix Turkish encoding
- Improve post templates
- Should add a styled arrow from the copy on the homepage to the cpu or the text box
- Make it easier to add blogs (i.e. I don't have to edit `src\collections\post.json` everytime I want to write a post, it can be just self contained within `src\pages\post`)
- Add RSS feed
- Look into adding private blogs with Substack integration
